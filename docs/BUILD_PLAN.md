# Gridiron Greatness Rating — Build Plan and Research Specification

Status: Proposal approved for planning. No code written yet.
Owner: Brian (1EyeBiney). Methodology is owned by the project lead and the coordinating Claude session; research agents supply evidence, never weights.
Last updated: 2026-09-16

## 1. Purpose

Build a rating system that measures how strong every NFL team actually was in each season, accounting for the true strength of its opponents and the competitive environment of its conference and era, and then use that database to evaluate Super Bowl participants against historical rankings.

The system answers: "How strong was this team really, given what it did, who it did it against, and how strong its competitive environment was?"

## 2. Decisions already made

These are settled. Do not reopen them without Brian's approval.

1. Scope begins with the 1970 season (post-merger). Super Bowls I through IV (1966–1969 seasons) are excluded from the core model because the AFL and NFL did not play each other, so the opponent network is disconnected. They may appear later as a separately labeled appendix.
2. Playoff games count toward True Strength. The rating uses every game a team played that season, regular season and postseason. The Super Bowl result itself is one more game, not a trophy bonus.
3. True Strength is a single schedule-adjusted network rating, not a weighted average of hand-picked components. The components from the original brainstorm (dominance, quality of competition, offense/defense balance, postseason, personnel) become descriptive sub-scores shown in each team's profile. They do not feed the headline number. This avoids triple-counting point margin.
4. Two headline scores per team-season: True Strength (how good) and Accomplishment (how successful). They are reported side by side and never blended.
5. Final home is a public GitHub repository under github.com/1eyebiney with a static website (GitHub Pages) for the results.
6. All outputs must be usable non-visually first: data tables, CSV, and plain-text narrative. Charts are optional extras and must never be the only way to get a number.
7. Everything normalized within its own season. Cross-era comparison uses standardized values (standard deviations from the league mean that year), never raw totals.
8. Only contemporaneous information may influence any score. No Hall of Fame status, no later coaching legacy, no "future" knowledge. Such facts may be displayed as descriptive context only.
9. True Strength Rating's model family is the Bayesian hierarchical margin model (src/models/bayesian_margin.py). Locked 2026-09-16 after Phase 2's within-season CV, Phase 4's walk-forward CV, and the adversarial audit all pointed the same direction - see docs/PHASE4_SUMMARY.md for the full case and the decision log below for Brian's confirmation.

## 3. Definitions

True Strength Rating (TSR). A team's estimated point-margin advantage over an average team from the same season on a neutral field, derived from a network model fit on all games that season, then expressed on a standardized scale so seasons can be compared. Higher is better. An average team is zero on the raw scale.

Accomplishment Rating (ACC). A score for how successful the season was: regular-season record and standing, division title, playoff wins, conference championship, Super Bowl result, undefeated season, and postseason dominance. This is where the trophy is rewarded.

Conference Strength Index (CSI). The estimated difference in average team strength between the two conferences in a given season, derived from the network model (interconference games are the informative edges). Reported with an uncertainty range because each team plays only a few interconference games.

Descriptive sub-scores (profile only, not in TSR): Schedule Difficulty, Dominance, Record vs Elite Opponents, Offense (standardized), Defense (standardized), Postseason Evidence, Roster Quality (contemporaneous), Coach Strength Entering Season.

## 4. Anti-bias rules

Hindsight rule: no variable may use information that did not exist by the end of that season.

Circularity rule: personnel honors (Pro Bowl, All-Pro) are partly a function of team success. They stay descriptive.

Double-count rule: any candidate variable must be checked for overlap with point margin and schedule strength before being reported alongside TSR.

Blowout rule: individual game margins are capped or dampened (candidate: cap at 28 points, or use a diminishing transform) so one 55–10 game does not dominate a season.

Tier rule: the all-time ranking uses only the Core model (variables available for every season since 1970). Modern-only data (play-by-play, EPA, explosive-play rate, from about 1999) lives in an Enhanced supplement and never changes the core ranking.

Small-sample rule: any conference-level or era-level statement carries an uncertainty range.

## 5. Model approach

Baseline (must reproduce first): Simple Rating System as published by Pro Football Reference. Reproducing their SRS numbers for several seasons proves our game data is clean. This is a validation step, not the final model.

Candidate core models, to be compared:

- Massey-style least squares on margin with home-field term and margin damping.
- Bradley-Terry / logistic model on win-loss with a margin-aware extension.
- Bayesian hierarchical margin model with shrinkage toward the league mean (preferred if it validates; gives uncertainty for free).
- Elo (season-restarted) as a sanity comparator, not a candidate for the headline.

Selection criterion: out-of-sample predictive accuracy on held-out games (log loss on winner, mean absolute error on margin), plus agreement with closing point spreads for seasons where spreads exist (roughly 1979 onward). The rating itself is retrodictive (fit on the full season), but the model family is chosen by predictive validation.

Standardization: convert each season's raw ratings to z-scores using that season's mean and standard deviation across all teams, then optionally map to a 0–100 display scale. Keep the z-score as the canonical value.

## 6. Era handling rules (to be finalized in Phase 3)

- Schedule length: 14 games (1970–1977), 16 games (1978–2020), 17 games (2021–). Standardization within season handles most of this; per-game rates used everywhere.
- 1982 strike: 9-game season, 16-team tournament. Include, flag, and report with wider uncertainty.
- 1987 strike: three replacement-player weeks. Decision needed: include, exclude, or down-weight those games. Default proposal: include but flag; test sensitivity.
- Expansion and realignment: 26 teams (1970), 28 (1976), 30 (1995), 31 (1999), 32 (2002). Conference and division membership tables by year are required inputs. 2002 realignment changes division structure.
- Franchise relocations and renames: a franchise ID table mapping every team-season to a stable franchise key.
- Ties: allowed; treat as half-win where record matters, and as zero margin in the network.
- Overtime and rule changes: noted for context, not modeled in Core.
- Neutral-site games (Super Bowls, international games): home-field term set to zero.

## 7. Data plan

Required for Core (1970–present):
- Every game: season, week, date, home team, away team, scores, neutral flag, playoff round flag, overtime flag.
- Team-season table: franchise key, conference, division, final record, division finish, playoff seed and results.
- Team-season totals: points for/against (derivable from games), and where available yards, turnovers.

Required for descriptive sub-scores:
- Pro Bowl, first- and second-team All-Pro, MVP/OPOY/DPOY by season.
- Approximate Value by roster (Pro Football Reference origin; licensing to be reviewed before redistribution).
- Head coach by team-season with prior career record.

Required for validation:
- Historical point spreads, roughly 1979 onward.
- Pro Football Reference published SRS, SOS, and margin for reproduction checks.

Required for Enhanced tier (1999–present):
- nflverse play-by-play (for EPA, explosive-play rate, turnover detail). Tests the idea2 hypothesis: explosive plays versus turnovers as a game-deciding trend.

Candidate sources (to be verified in Phase 1): nflverse schedule and play-by-play data; public historical score archives covering 1970 onward; Pro Football Reference used as a manual validation reference rather than a scraped pipeline because of terms of service and rate limits; publicly posted spread datasets. Every source gets a provenance note and license check before use. Raw downloads are stored under data/raw and never edited by hand; all cleaning is scripted.

## 8. Phases

Phase 0 — Specification (this document). Done when Brian approves it and it is committed to the repo.

Phase 1 — Data acquisition and audit. Deliverable: a verified games table for 1970 through the most recent completed season, with a data-quality report (team counts per season, games per team, score sanity checks, playoff bracket reconciliation). Exit check: SRS reproduced for at least five seasons across different decades within rounding.

STATUS: COMPLETE as of 2026-09-16. See docs/PHASE1_SUMMARY.md for the full report. Headline result: 13,685 games unified from FiveThirtyEight (1970-1998, MIT license) and nflverse (1999-2025, CC BY 4.0), all data-quality checks passed, and our SRS matched Pro Football Reference's published values within 0.05 points (max) across 148 team-seasons in 5 different decades - consistent with PFR's own display rounding. Two known items carried into Phase 2+: pre-1999 playoff games lack round labels (WC/DIV/CON/SB), and there is no team-season conference/division table yet (needed for Phase 5's Conference Strength Index).

Phase 2 — Baseline and candidate models. Deliverable: SRS reproduction, then Massey, Bradley-Terry, and Bayesian implementations producing per-season ratings; a comparison report.

STATUS: COMPLETE as of 2026-09-16. See docs/PHASE2_SUMMARY.md for the full report. Headline result: all four models (Massey, Bradley-Terry, Bayesian hierarchical margin, and Elo as a sanity comparator) fit on every 1970-2025 game including playoffs; 5-fold in-season cross-validation (prequential for Elo) shows the Bayesian model narrowly ahead on log loss and margin MAE in every era slice, matching this document's stated preference "if it validates" - though this is Phase 2's easier in-season test, not Phase 4's real validation, so no model family is locked in yet. Fitted home-field advantage averaged 2.42 points/game, in line with common NFL estimates. One real bug was caught and fixed along the way: unregularized Bradley-Terry MLE diverges under quasi-complete separation for teams unbeaten/winless within a small cross-validation fold; a small fixed L2 penalty (documented in src/models/bradley_terry.py) fixed it.

Phase 3 — Era normalization. Deliverable: standardization layer, strike and expansion rules implemented, sensitivity report showing how much each rule moves rankings.

STATUS: COMPLETE as of 2026-09-16. See docs/PHASE3_SUMMARY.md and docs/PHASE3_ERA_NORMALIZATION.md for the full report. Every model's rating is now also expressed as a within-season z-score (canonical cross-era value, per section 5) plus an optional 0-100 display scale. Four of section 6's seven era rules turned out to already be satisfied by how Phase 1/2 were built (schedule length, expansion/realignment, franchise relocations, ties/overtime/neutral sites) - confirmed, not assumed. The 42 specific 1987 replacement-player games were identified from the data itself (a clean gap where the strike-cancelled week would have been) and flagged; refitting 1987 with vs. without them gives a Spearman rank correlation of 0.88-0.90, supporting the default of including them. 1982's Bayesian posterior uncertainty is confirmed wider than every neighboring season (3.05 vs. a 2.26-2.83 range for 1978-1986), satisfying the small-sample rule without special-casing.

Phase 4 — Validation. Deliverable: out-of-sample prediction results; spread comparison; adversarial audit listing teams whose ratings conflict with historical evidence and an explanation or fix for each. Model family locked at the end of this phase.

STATUS: COMPLETE (spread comparison deferred, not blocking) as of 2026-09-16. Out-of-sample validation and adversarial audit complete; model family LOCKED - Bayesian hierarchical margin model, confirmed by Brian. See docs/PHASE4_SUMMARY.md for the full picture, docs/PHASE4_PART1_SUMMARY.md and docs/PHASE4_WALKFORWARD_VALIDATION.md for validation, docs/PHASE4_ADVERSARIAL_AUDIT.md for the audit.

Genuine walk-forward validation (weekly refits using only that season's earlier games, zero future leakage) replaced Phase 2's within-season 5-fold CV. Bradley-Terry's REG_STRENGTH was re-selected honestly on 1970-1998 only and landed on exactly Phase 2's value (3.0). Building this surfaced and fixed three real bugs (floating-point-noise sigma, missing parameter uncertainty in win-probability predictions, LOO shrinkage selection going backwards on tiny samples - all in src/models/common.py and src/models/bayesian_margin.py) plus one structural, unfixed finding: unregularized Massey is unreliable for roughly the first 10 weeks of a season because the schedule graph starts disconnected and plain least squares reports zero uncertainty in directions the data says nothing about, while the regularized models handle this correctly by construction.

The spread comparison was paused: every free bulk historical-spread dataset found traces back to sources with redistribution restrictions (spreadspoke.com's Terms of Use, underlying the popular Kaggle mirror, prohibits resale/redistribution without permission) - the same provenance gap Phase 1 hit with Pro Football Reference. Given a choice between a hand-verified sample, a source Brian provides, or skipping it, Brian chose to skip it for now; it can be revisited later without disturbing anything else.

The adversarial audit (src/adversarial_audit.py, src/run_phase4_audit.py) checked the Bayesian model three ways: rating-vs-record residuals computed entirely from the game log (explained by margin of victory/defeat in every case, not a defect), cross-model disagreement (Elo the most frequent outlier, expected), and a benchmark check against ten historically undisputed team-seasons - every one landed where expected, including the two hardest cases: the defense-carried, weak-offense 2000 Ravens still rank #1 in their season, and the 2011 Giants (Super Bowl champion, 9-7 regular season) rank a believable 9th of 32, the exact gap the separate True Strength / Accomplishment scores are designed to show.

Across every slice of Phase 2, Phase 4's walk-forward validation, and this audit, nothing argues against the Bayesian model and several things argue for it. This session recommends it as the project's True Strength Rating, but per the decision log, locking the model family is Brian's call with the coordinating session, not this report's.

Phase 5 — Accomplishment, Conference Strength Index, profiles. Deliverable: ACC formula documented and computed; CSI per season with uncertainty; full team-season profile table including descriptive sub-scores.

STATUS: COMPLETE as of 2026-09-16, ACC formula pending Brian's review (recommendation, not a lock - see docs/PHASE5_SUMMARY.md). Built the team-season conference/division table finally (Phase 1's carried-forward item), verified against multiple independent sources for all six realignments 1970-2025 and cross-checked its team counts against Phase 1's own audit with zero mismatches. CSI is a linear contrast of the locked model's fitted rating vector, with uncertainty from that same posterior - not a separate calculation. ACC is a documented point schedule (src/accomplishment.py) built without needing pre-1999 playoff round labels, since the Super Bowl is reliably identifiable as each season's single latest-dated game across all 56 seasons with zero exceptions. Five of eight descriptive sub-scores are computed (Dominance, Offense, Defense, Schedule Difficulty, Record vs Elite Opponents); Roster Quality and Coach Strength Entering Season are not - both need external data this project hasn't acquired, Roster Quality with the same open redistribution-rights question as the deferred spread comparison. Validation: 2007 NE (highest True Strength Rating ever, TSR z=2.51) scores ACC=24.0, not the ceiling, because they lost the Super Bowl; 2011 NYG (a middling TSR z=0.76) scores ACC=24.6, nearly matching it, because they won it - exactly the gap the two separate headline scores exist to show. 2000 BAL correctly shows division_title=False despite winning the Super Bowl, reflecting that they were a real wild-card champion - not hard-coded, it falls directly out of the new division table.

Phase 6 — Outputs and website. Deliverable: CSV and JSON exports; per-Super Bowl matchup pages; query pages (greatest champions, weakest champions, greatest losers, best teams never to win, largest mismatches, greatest conference imbalance, best decade, records that most overstated strength); accessible static site on GitHub Pages with proper headings, data tables with header cells, and text alternatives for any chart.

STATUS: BUILT as of 2026-09-16, not yet published - see docs/PHASE6_SUMMARY.md. The static site (src/build_site.py, templates in src/site_templates/) generates a landing page, methodology page, 56 season pages, 56 Super Bowl pages, all 8 query pages, and a data-download page, entirely from data already in data/processed/ and data/reference/. Built to the letter of the section 10 accessibility spec (semantic landmarks, real tables with scope attributes, skip link, deliberately no charts - every number is in a plain table and downloadable) but NOT tested with NVDA/JAWS as section 10 also calls for, since that needs a human with that software. A GitHub Actions workflow (.github/workflows/deploy-pages.yml) is ready and tested locally but two things were deliberately left undone pending explicit confirmation: enabling GitHub Pages in repo settings, and pushing to master to trigger the first live deploy - both are publishing actions this project's safety guidelines require confirming separately from writing the code that enables them.

## 9. Tooling (confirmed 2026-09-16)

Brian has delegated the language choice. Decision: Python 3 with pandas and numpy for data work; scipy or a lightweight Bayesian library for models; pytest for tests; data stored as CSV/Parquet in the repo; a static site generator or plain HTML templates for GitHub Pages. Repo layout proposal:

    data/raw/          untouched downloads with source notes
    data/processed/    scripted outputs
    src/               pipeline and models
    tests/             reproduction and sanity tests
    docs/              this plan, methodology, decision log
    site/              generated website

## 10. Accessibility requirements for the site

Semantic headings for every section; real HTML tables with scope attributes; sortable tables must be keyboard-operable and announce sort state; every chart accompanied by the underlying table or a text summary; no information conveyed by color alone; skip links; tested with NVDA and JAWS before release.

## 11. Open questions

- Confirm handling of 1987 replacement games (default: include and flag).
- Choose the display scale for TSR (z-score only, or also 0–100).
- Decide whether Approximate Value can be redistributed or must be computed on the fly from source.
- Decide the exact ACC point schedule (to be drafted in Phase 5).

## 12. Decision log

2026-09-16 — Scope starts 1970. Playoffs count in TSR. Single network rating, components descriptive only. GitHub repo plus GitHub Pages site. Build document maintained in project and repo.
2026-09-16 — Python confirmed as pipeline language (Brian delegated the choice). Phase 1 may be run on any model; model-selection decisions in Phases 2–4 should be made by the coordinating session with Brian, not left to a research agent.

## 13. Notes for the next session

Phase 6 is built but not published - see status note in section 8 and docs/PHASE6_SUMMARY.md. Next session's first job, before anything else: check whether Brian has enabled GitHub Pages (repo Settings -> Pages -> Source: GitHub Actions) and whether master has been pushed since .github/workflows/deploy-pages.yml was added - if not, that's still the very next step, not a new task. Once live, do an actual NVDA/JAWS pass (BUILD_PLAN section 10 calls for it; this session could only build to the spec, not test with real screen reader software). The ACC point schedule (Phase 5) is still a recommendation, not settled - unlike the model family, BUILD_PLAN never specified its point values, so there's nothing to "confirm" so much as react to; accomplishment.py is a pure function of data already in the repo, so changes are cheap whenever Brian gets to it. Three smaller open items, still none blocking: (1) reconstruct playoff round labels for 1970-1998 games; (2) Roster Quality and Coach Strength Entering Season sub-scores need new external data with a provenance check; (3) the deferred spread comparison. None of these three block anything - Phases 0 through 6 are functionally complete once Pages is live.

2026-09-16 — Phase 1 completed by Claude Sonnet 5. Repo built at github.com/1eyebiney (local working copy only so far; not yet pushed - this session has no GitHub credentials or shell access on Brian's machine, so Brian will need to push it himself, or connect a GitHub integration in a future session).

2026-09-16 — GitHub push handoff: this cloud session's network policy blocks github.com's web/API/OAuth endpoints (only plain git clone/push over HTTPS reaches GitHub - confirmed by testing, not assumed), so the device-code login Brian remembers using before cannot run here. Decision: Brian is switching to a local Claude Code session on his own machine to do the push and continue Phase 2+, since a local session has unrestricted network access and likely still has cached GitHub credentials from before.

HANDOFF FOR THE LOCAL SESSION: a git bundle with the complete Phase 1 commit history (7 commits) was written to C:\nbs\gridiron-greatness-rating\gridiron-greatness-rating.bundle. To restore it: `git clone gridiron-greatness-rating.bundle gridiron-greatness-rating-repo` (or `git init` in place, `git bundle unbundle` won't work in a non-empty dir - cleanest is to clone the bundle into a fresh directory, then copy the resulting .git folder over the existing C:\nbs\gridiron-greatness-rating folder, or just work out of the cloned directory going forward). Then create the empty repo on github.com/1eyebiney (public, no README/gitignore/license initialization to avoid conflicts), `git remote add origin https://github.com/1eyebiney/gridiron-greatness-rating.git`, and `git push -u origin master`. After that, Phase 2 (see section 8) picks up normally.

2026-09-16 — GitHub push completed by a local Claude Code session on Brian's machine, which had `gh` already authenticated as 1EyeBiney (`repo` scope) — no OAuth flow needed. Restored the bundle's 7 commits by cloning it into a scratch directory and copying `.git` over the working copy; the working-copy files had picked up CRLF line endings (Windows `core.autocrlf=true` at the system git config level) while the bundle's blobs were LF, which briefly showed every file as modified. Fixed by normalizing to LF and adding `.gitattributes` (`* text=auto eol=lf`) so this doesn't recur, added `*.bundle` to `.gitignore`, then `gh repo create 1eyebiney/gridiron-greatness-rating --public` and pushed master. Repo is live at https://github.com/1EyeBiney/gridiron-greatness-rating (GitHub normalized the path casing to the account name). Three pre-existing local files were left untracked, not part of the repo: a duplicate top-level `BUILD_PLAN.md` (docs/BUILD_PLAN.md is canonical), `idea2.txt`, and `initial prompt for gridiron greatness rating.txt` — Brian's working notes, kept local-only until he decides otherwise. Phase 2 starts now.

2026-09-16 — Phase 2 completed by the same local session, same day. This machine had numpy and scipy but not pandas or pytest; both were pip installed and pinned in a new requirements.txt. Built src/models/{massey,bradley_terry,bayesian_margin,elo}.py, src/evaluate.py (5-fold in-season cross-validation for the three batch models, prequential for Elo), src/run_phase2.py (fits all four for every 1970-2025 season and writes the comparison report), and tests/test_models.py (11 sanity tests on synthetic data, run via pytest). Caught and fixed a real bug along the way: unregularized Bradley-Terry MLE diverged under quasi-complete separation in small cross-validation folds, driving log loss to 0.81 (worse than a coin flip) until a small fixed L2 penalty was added. Full results and interpretation in docs/PHASE2_SUMMARY.md and docs/PHASE2_MODEL_COMPARISON.md; new data files at data/processed/{team_season_ratings,model_params,phase2_cv_results}.csv. Phase 3 starts now.

2026-09-16 — Phase 3 completed by the same local session, same day. Built src/standardize.py (within-season z-scores plus an optional 0-100 display scale, applied in place to team_season_ratings.csv), src/era.py (identifies the 42 1987 replacement-player games from a date gap in the data, cross-checked against the historical record; writes data/reference/season_metadata.csv), and src/sensitivity.py + src/run_phase3.py (the two sensitivity checks section 6 calls for). Caught one bug in the report generator itself before committing: a hardcoded "above 0.9" claim about the 1987 sensitivity correlation was actually wrong for one of the two models (0.884, not above 0.9) - the fixed version computes the claim from the actual numbers instead of asserting a round one. Full results in docs/PHASE3_SUMMARY.md and docs/PHASE3_ERA_NORMALIZATION.md; 6 new tests in tests/test_era.py (17 total, all passing). Phase 4 starts now.

2026-09-16 — Pre-Phase-4 review (Brian switched the session to Claude Fable 5.1 specifically to get a second model's eyes on Phases 2-3 before locking anything in). Four fixes applied and Phases 2-3 re-run: (1) the Bayesian model's shrinkage penalty was chosen from a grid so coarse that only 7 distinct values were ever selected across 56 seasons, quantizing every reported standard error - now refined with a bounded 1-D search between the bracketing grid points (56 distinct values; 1982 SE moved 2.97 to 3.05, aggregate log loss unchanged to 4 decimals). (2) Bradley-Terry's REG_STRENGTH was tuned against the same folds Phase 2 then scored, a mild test-set leak (~0.003 log loss at most); left as-is for Phase 2 but flagged in the docstring, and fit_bradley_terry now takes reg_strength as a parameter so Phase 4 can re-select it inside its training window. (3) run_phase2.py silently drops Phase 3's z-score columns if re-run alone - added src/run_all.py as the safe entry point and documented the ordering. (4) Massey was mislabeled "weighted least squares" in two docs; it is ordinary least squares. Also added: the 1987 replacement-week records per team, computed from our data, which match the historical record (PHI and NYG 0-3, WSH 3-0) and explain exactly which teams the sensitivity check moves and why. Review verdict: nothing blocks Phase 4; Bayes and Massey z-scores agree at Spearman 0.995, so the Phase 4 model-family decision is really about shrinkage strength, not family.

2026-09-16 — Phase 4 Part 1 (walk-forward validation) completed by Claude Sonnet 5, session continued from the Fable review above per Brian's request to switch back and proceed. Built src/schedule_weeks.py (a continuous week index per season, from nflverse's own column for 1999-2025 and date-gap inference for 1970-1998), src/walkforward.py (weekly refit-and-predict-next-week for the three batch models, zero future leakage - Elo needed no changes), and src/run_phase4_validation.py (re-selects Bradley-Terry's REG_STRENGTH on 1970-1998 only, before touching 1999-2025; it landed on exactly Phase 2's value, 3.0). The first full run showed Massey and the Bayesian model with log loss above 1.0 - worse than a coin flip - and chasing that down (rather than dismissing it as noise, per the standing practice from the Fable review above) found three distinct real bugs: (1) near-zero sigma from floating-point roundoff on tiny fits, turning noise into unstable probabilities - fixed with a documented floor, models/common.py MIN_SIGMA; (2) the deeper issue - win probability used a model's flat in-sample sigma with no account for uncertainty in the fitted ratings themselves, badly overconfident early in a season - fixed properly with models/common.py:predictive_sigma, the standard prediction-interval formula using each model's actual parameter covariance, now used in both evaluate.py and walkforward.py; (3) the Bayesian model's leave-one-out shrinkage selection went backwards on a single week's ~13-16 games (alpha=0.012, versus 2.4-16.7 for every full-season fit) - fixed with models/bayesian_margin.py MIN_ALPHA, floored below the smallest alpha any full-season fit has ever needed. A fourth finding was reported rather than patched: unregularized Massey stays unreliable through roughly week 10 of every season because the schedule graph starts as isolated pairs and plain least squares reports zero uncertainty in directions the data says nothing about - not a bug, a structural property of unregularized OLS, and real evidence for the plan's stated preference for the Bayesian model. Rerunning Phase 2-3 after these model-code fixes changed their numbers only trivially (Massey's Phase 2 log loss improved 0.6341 to 0.6283; nothing else moved outside noise) - both reports left as originally written since the conclusions didn't change. Headline: the Bayesian model has the lowest log loss in every slice of both Phase 2 and this validation except one statistically meaningless tie - the most convergent evidence yet, though the model family is not locked until the spread comparison and adversarial audit are done. Full writeup in docs/PHASE4_PART1_SUMMARY.md and docs/PHASE4_WALKFORWARD_VALIDATION.md; 4 new test files (28 tests total, all passing). Spread comparison is next, but paused for Brian's input on a data source before anything is fetched or ingested, per the same provenance standard Phase 1 applied to FiveThirtyEight and nflverse.

2026-09-16 — Phase 4 spread comparison and adversarial audit, same session. Researched historical NFL spread datasets (WebSearch/WebFetch, read-only, nothing downloaded) before asking Brian how to proceed, per the pause above: the most commonly used free option (Kaggle's tobycrabtree/nfl-scores-and-betting-data, spreads from 1979 on) traces back to spreadspoke.com, whose Terms of Use prohibit resale/redistribution without permission - a Kaggle uploader's license tag doesn't establish they had the right to grant one. Given a choice between a hand-verified sample (matching Phase 1's PFR approach), a source Brian provides, or skipping it, Brian chose to skip it for now. Built the adversarial audit instead (src/adversarial_audit.py, src/run_phase4_audit.py): rating-vs-record residuals computed entirely from the game log (largest gaps in both directions, all explained by margin of victory/defeat, not a defect - the intended behavior of a margin-based rating), cross-model disagreement (Elo the most frequent outlier, expected), and a ten-team benchmark check against historically undisputed team-seasons (1972 Dolphins, 1985 Bears, 2007 Patriots, 2008 Lions 0-16, etc.) - every one landed where expected, including the two hardest cases: the defense-carried 2000 Ravens still rank #1 in their season despite a weak offense, and the 2011 Giants (Super Bowl champion, 9-7 regular season) rank a believable 9th of 32, the exact gap the separate True Strength/Accomplishment scores exist to show, not close. Recommendation (not a lock, per the governance note above): proceed with the Bayesian hierarchical margin model as the project's True Strength Rating. Full writeup in docs/PHASE4_SUMMARY.md and docs/PHASE4_ADVERSARIAL_AUDIT.md; 2 new tests (30 total, all passing). Phase 4 is now functionally complete pending Brian's confirmation of the model-family recommendation; Phase 5 (Accomplishment, Conference Strength Index, profiles) is next once confirmed.

2026-09-16 — Brian confirmed the model-family recommendation: "yes, go with your recommendation." True Strength Rating's model family is locked to the Bayesian hierarchical margin model (section 2, item 9). Phase 4 is complete (spread comparison deferred, not blocking). Phase 5 starts now.

2026-09-16 — Phase 5 completed by the same local session, same day. Built the team-season conference/division table (src/division_alignment.py) that's been a carried-forward item since Phase 1 - verified via web search against Wikipedia, the Pro Football Hall of Fame, and team history pages rather than trusted to memory, covering all six realignments in this window; its team counts per era matched Phase 1's independently-audited numbers exactly, with zero mismatches. Built CSI (src/conference_strength.py) as a linear contrast of the locked Bayesian model's fitted rating vector, with its uncertainty range derived from that same posterior rather than computed separately. Built ACC (src/accomplishment.py) as a documented, from-scratch point schedule - genuinely open per section 11 ("to be drafted in Phase 5," no point values specified anywhere), so this is a recommendation for Brian to react to, not a locked decision like the model family. Discovered the Super Bowl is reliably identifiable as each season's single latest-dated game with zero exceptions across all 56 seasons, which let ACC's Super Bowl bonus work correctly without needing the still-missing 1970-1998 playoff round labels; other playoff wins get a flat bonus (not escalating by round) for the same reason, documented as a known simplification. Built five of BUILD_PLAN's eight descriptive sub-scores (src/team_profile.py) - Dominance, Offense, Defense, Schedule Difficulty, Record vs Elite Opponents - purely from the game log and the locked model's own ratings; Roster Quality and Coach Strength Entering Season need external data this project hasn't acquired and aren't built. Validation highlights: 2007 NE (highest True Strength Rating on record) scores ACC=24.0, not the ceiling, because they lost the Super Bowl, while 2011 NYG (a middling regular season) scores ACC=24.6 because they won it - the intended TSR/ACC gap, not a bug; 2000 BAL correctly shows division_title=False despite being that year's Super Bowl champion, reflecting their real wild-card run, which fell out of the new division table without being hard-coded. Full writeup in docs/PHASE5_SUMMARY.md; 4 new test files (31 new tests, 61 total, all passing). Phase 6 (outputs and the GitHub Pages site) is next, once Brian has had a chance to react to the ACC formula.

2026-09-16 — Phase 6 built by the same local session, same day (Brian said "proceed" without ACC changes, so it shipped as drafted). Built all 8 query pages (src/queries.py), reusing the same True Strength z-score already validated in Phase 4's audit for every ranking. Built the static site generator (src/build_site.py, Jinja2 templates in src/site_templates/): landing page, plain-language methodology page, 56 season pages, 56 Super Bowl pages, 8 query pages, a data-download page - previewed locally with a local HTTP server and verified via get_page_text/read_page (accessibility tree, landmark structure, table scope attributes) rather than just trusted to compile cleanly; caught and fixed one real bug this way (every page except the query pages had an empty <title> tag - the base template's title block wasn't being set by most render() calls, fixed by giving every child template its own {% block page_title %} instead of relying on every caller to remember to pass one). Built to the letter of BUILD_PLAN section 10's accessibility spec (semantic landmarks, real tables with scope attributes, skip link, no charts at all - satisfying "usable non-visually first" more directly than an accessible chart would) but explicitly NOT tested with NVDA/JAWS as that section also calls for, since that needs a human with that software, not this session. Added .github/workflows/deploy-pages.yml, tested locally by running the exact command CI will run (`python src/run_phase6.py` from the repo root). Two things deliberately left undone pending Brian's explicit go-ahead, not because of any technical blocker: enabling GitHub Pages in repo settings, and pushing to master to trigger the first live deploy - both are publishing actions this project's safety guidelines call out as needing separate confirmation from writing the code that makes them possible. Full writeup in docs/PHASE6_SUMMARY.md; 4 new tests (72 total, all passing).

2026-09-16 — Live-site bug reported by Brian minutes after the first deploy: every link on the query index page 404'd. Root cause: queries_index.html built its links from the query's raw underscore slug ("greatest_champions"), but build_site.py wrote the actual files with hyphens ("greatest-champions.html") - two pieces of code, each individually correct with respect to its own inputs, silently disagreeing with each other. Fixed by computing the hyphenated slug once and using it for both the filename and every link (src/build_site.py). Added a real regression test for this class of bug rather than just the one instance: tests/test_build_site.py now builds the entire site to a temp directory and checks every internal href on every page resolves to a file that actually exists - the kind of check a template-level or function-level unit test structurally cannot catch, since each side of this bug was locally correct. 73 tests total, all passing. Pushed; the deploy workflow republishes automatically.
